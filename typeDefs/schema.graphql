type Query {
    name (name:String) : String!
}

type Mutation {
    addRoom (
        title:String,
        hostID:String,
        star:Int,
        img:String,
        price:Int,
        review:Int,
        capacity:Int
    ) : Boolean
}